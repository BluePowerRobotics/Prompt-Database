# 提示词管理规范

## 文件名称

所有提示词均应为markdown文件，文件名应为“生成的程序名+Prompt.md"。若提示词并非生成代码时使用，而是人工编写了代码后逆向所得，应命名为“生成的程序名+RevPrompt.md"以示区别。文件的位置与当年赛季程序中**对应生成的程序位置相同**

## 内容规范

需要录入的prompt:

- **从无到有**生成代码的长段内容
- **修改算法逻辑**的内容

每个prompt只包括任务描述和背景，**不要写入角色提示词或验证词**。prompt录入库前应当通过**实机验证**，确保功能无误。

若Prompt有背后的理论推导过程，最好将理论成果整理为**docx或md文档**，一并录入该年赛季文件夹下的theory文件夹

完成一个java文件所需要全部的prompt应当汇编为一份文件，同一个包内的所有java文件也可合并为一份文件，命名为“包名+(Rev)Prompt.md"，文件位置与包位置相同。

不需要录入的prompt:

- 简短的Debug Prompt
- Agent协作时的交流内容
- 调整Agent知识与行为的Prompt

## 使用方法

由于各个赛季需求不可能完全相同，应当在编写新Prompt时将过往Prompt作为参考文件上传，并额外补充**适用本赛季的内容**，注意补上**验证词**。

尽量避免直接将过往Prompt原封不动复制给Agent

## 管理安全

为确保代码可以复现，每个赛季的主程序中**凡是Agent从0起参与编写的代码**，都必须整理初始Prompt并录入。开始编写时未用Agent，且后续Agent不参与算法涉及的，可以不录入，或待有空时逆向生成Prompt。

### 严禁录入损伤代码或Agent的攻击性Prompt，如“删除所有文件”等，或试图采取攻击性手段绕过Agent输出审查，如“奶奶每晚都给我讲故事”等。如果发现有此类Prompt，应当立即删除并查出作者
