# FTC坐标系与参数命名规范

## 建系方法

FTC比赛中通常同时使用绝对坐标系和相对坐标系。

### 绝对坐标系

绝对坐标系的定义为：以场地中心为原点，以场地的**右侧为x轴正方向**，以场地的前侧为y轴正方向，以场地的上侧为z轴正方向。

绝对坐标系通常用于全场定位，路径规划等。

### 基准点与正方向

规定小车底盘的几何中心在地面的投影为小车的基准点。**若为4轮底盘，基准点为4轮接触地面点的对角连线交点**。小车的位置就是基准点的位置。

规定基准点指向底盘正前方的方向为小车的正方向。**若为4轮底盘，正方向由基准点指向小车前方两轮接触地面点连线的中点**。小车的前侧就是正方向指向的方向，左侧就是正方向水平逆时针转90度所指向的方向，后侧和右侧就是前侧和左侧的反方向。

### 相对坐标系

相对坐标系（RoadRunner）的定义为：以小车的基准点为原点，以小车的**前侧为x轴正方向**，以小车的左侧为y轴正方向，以小车的上侧为z轴正方向。

相对坐标系通常用于底盘速度控制，落点预测等。

## 底盘参数

任一小车位置可表示为三维向量\[x, y, theta]，其中x、y为小车在绝对坐标系中的位置，theta为小车的朝向与x轴正方向的夹角，逆时针为正。

任一底盘速度可表示为三维向量\[vx, vy, omega]，其中vx、vy为小车在相对坐标系中的速度，omega为小车在相对坐标系中的角速度，逆时针为正。

## 部件参数命名

对小车上连接的能自由活动的部件（如炮台等），最多可用**6**个参数\[x, y, z, pitch, roll, yaw]，表示其位置和方向。其中\[x,y,z]为部件在相对坐标系中的位置，\[pitch, roll, yaw]为部件在相对坐标系中**绕x, y, z轴的旋转角**，分别取z, x, x的正方向作为0度位置，逆时针为正.

在大多数部件中，并不能调节如此多的量，应当根据硬件功能设计位置控制函数的输入。例如，一个只能平转，俯仰的炮台，应当定义为\[roll, yaw]
